# CodeQL advanced-setup configuration for .github/workflows/codeql.yml.
#
# Query selection stays in the workflow's matrix (`queries: security-extended`);
# this file carries the analysis name and any bound on what gets extracted.
#
# Nothing is bounded. The one entry this file ever held was `mango-protocol/`,
# the standalone protocol repository imported with its history and waiting to be
# relocated; the relocation dropped the directory and the entry with it, and the
# protocol's TypeScript is scanned with the rest of the repository from here on.
name: mangostudio
