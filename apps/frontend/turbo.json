{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**", "dist-metafile.json"],
      "env": ["MANGO_API_URL", "VITE_*"]
    }
  }
}
