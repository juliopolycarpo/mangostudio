export { messages as en } from './en';
export { messages as ptBR } from './pt-BR';
export type { Locale, Messages } from './types';

/** Locale padrão da aplicação. */
export const defaultLocale = 'en' as const;
