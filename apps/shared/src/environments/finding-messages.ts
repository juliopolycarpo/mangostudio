/**
 * English environment finding templates for non-React consumers (CLI).
 * Sourced from the shared i18n dictionary so UI and CLI stay aligned.
 */

import { en } from '../i18n';

export const environmentFindingTemplatesEn = en.environments.findings;
export const environmentDisplayNamesEn = en.environments.names;
export const environmentLtsLabelsEn = en.environments.lts;
