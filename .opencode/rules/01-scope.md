# Scope

These rules are only for OpenCode behavior in this repository.

- Keep `.opencode/` instructions focused on OpenCode configuration and local tooling.
- Prefer repo-specific facts from `opencode.json` over generic advice.
- Do not restate application architecture, coding standards, or product rules here.
- Use the root `AGENTS.md` for project-wide development guidance.
