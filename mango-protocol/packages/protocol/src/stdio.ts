/**
 * `@mangostudio/protocol/stdio`: the NDJSON transport over standard input and
 * standard output. This entry may import `node:`; the core entry may not.
 */

export { type StdioPortOptions, stdioPort } from './transports/stdio';
