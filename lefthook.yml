pre-commit:
  parallel: true
  commands:
    biome:
      glob: '*.{ts,tsx,js,jsx,json,yml,yaml}'
      run: bunx biome check --write {staged_files}
      stage_fixed: true
    typecheck-affected:
      run: bun run check --staged --skip-lint --skip-format
      skip:
        - merge
        - rebase
